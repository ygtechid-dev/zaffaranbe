version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: zafaran_db
    restart: always
    environment:
      MYSQL_DATABASE: zafaran_db
      MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
